* [x] `ai-service` 独立项目结构已创建

- [x] OCR 接口 `/v1/ocr/scan` 可正常识别图片文字

- [x] 人脸注册接口 `/v1/face/register` 可正常存入向量库

- [x] 人脸识别接口 `/v1/face/recognize` 可正常识别身份

- [x] 视频分析接口 `/v1/video/analyze` 可正常分析视频并输出人脸时间点

- [x] 所有接口均可通过 API Key 进行鉴权访问

- [x] 独立的 `Dockerfile` 和 `docker-compose.yml` 可成功构建并启动服务

